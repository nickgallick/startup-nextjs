collections_config:
  posts:
    name: "Posts"
    path: "src/_posts"
    parser: "JSON"
    output: true
    url: "/posts/[slug]"
  pages:
    name: "Pages"
    path: "pages"
    parser: "JSON"
    output: true
    url: "/[slug]"

