collections_config:
  posts:
    name: "Posts"
    path: "src/_posts"
    parser: "JSON"
    output: true
    url: "/posts/[slug]"
  pages:
    name: "App"
    path: "app"
    parser: "JSON"
    output: true
    url: "/[slug]"

