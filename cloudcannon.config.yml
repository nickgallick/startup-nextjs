collections_config:
  posts:
    name: "Posts"
    path: "src/_posts"
    parser: "frontmatter"
    output: true
    url: "/posts/[slug]"
  pages:
    name: "App"
    path: "app"
    parser: "frontmatter"
    output: true
    url: "/[slug]"

